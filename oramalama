#!/bin/bash
#
# oramalama — local AI coding assistant powered by RamaLama
# https://github.com/containers/ramalama
#
# Subcommands: run  serve  launch  list  pull  ps  rm  search
# Global flags: --remote <url>  --model <name>  --dry-run

# ---------------------------------------------------------------------------
# Configuration defaults
# ---------------------------------------------------------------------------
CONTAINER_NAME="ramalama-opencode"
LOCAL_HOST="127.0.0.1"
LOCAL_PORT="8080"
OPENCODE_CONFIG="${HOME}/.config/opencode/opencode.json"
GOOSE_CONFIG="${HOME}/.config/goose/config.yaml"

# Default model: Gemma 4 31B (dense, best coding quality) at Q4_K_M quant (~18GB)
DEFAULT_MODEL="hf://unsloth/gemma-4-31B-it-GGUF:Q4_K_M"

# Systemd quadlet service name for the default model (persistent, auto-restart).
# Other models still use ad-hoc ramalama serve.
QUADLET_SERVICE="ramalama-opencode.service"

# Ports probed when scanning for remote ramalama / Ollama servers.
DISCOVER_PORTS=(8080 11434 8081)

# ---------------------------------------------------------------------------
# User config file: ~/.config/oramalama/config
# Loaded before CLI flags so flags always win. Supported keys (bash syntax):
#   RAMALAMA_ENDPOINT=http://myserver:8080
#   DEFAULT_TOOL=opencode
#   DEFAULT_MODEL=hf://unsloth/gemma-4-31B-it-GGUF:Q4_K_M
# ---------------------------------------------------------------------------
USER_CONFIG="${HOME}/.config/oramalama/config"
# shellcheck source=/dev/null
[ -f "$USER_CONFIG" ] && source "$USER_CONFIG"

# ---------------------------------------------------------------------------
# check_deps: verify required tools are installed; exit with friendly hints
# if any are missing. llmfit is optional — silently tolerated here.
# ---------------------------------------------------------------------------
check_deps() {
    local missing=()
    for dep in ramalama gum jq curl; do
        command -v "$dep" &>/dev/null || missing+=("$dep")
    done

    if [ ${#missing[@]} -gt 0 ]; then
        echo "❌ Missing required dependencies:"
        for dep in "${missing[@]}"; do
            case "$dep" in
                ramalama) echo "   ramalama  → https://github.com/containers/ramalama" ;;
                gum)      echo "   gum       → https://github.com/charmbracelet/gum" ;;
                jq)       echo "   jq        → https://jqlang.github.io/jq/download/" ;;
                curl)     echo "   curl      → install via your system package manager" ;;
            esac
        done
        exit 1
    fi
}

# ---------------------------------------------------------------------------
# detect_hardware: set USE_IMAGE and EXTRA_SERVE_FLAGS based on local GPU.
#
# Strix Halo (AMD Ryzen AI Max / RDNA 4) is an APU where the BIOS carves out
# a large chunk of system RAM as GPU VRAM (typically >60 GB). No discrete GPU
# currently ships with >60 GB VRAM, so this is a reliable heuristic.
#
# For all other hardware ramalama picks its own optimised image automatically
# (CUDA for NVIDIA, ROCm for standard AMD dGPUs, CPU fallback, etc.), so we
# just leave USE_IMAGE empty and let ramalama decide.
# ---------------------------------------------------------------------------
detect_hardware() {
    USE_IMAGE=""          # empty = let ramalama choose the container image
    EXTRA_SERVE_FLAGS="--jinja"
    IS_STRIX_HALO=false

    for card in /sys/class/drm/card*/device; do
        local vendor_path="${card}/vendor"
        local vram_path="${card}/mem_info_vram_total"
        if [[ -f "$vendor_path" && -f "$vram_path" ]]; then
            local vendor vram_bytes vram_gb
            vendor=$(cat "$vendor_path" 2>/dev/null)
            vram_bytes=$(cat "$vram_path" 2>/dev/null)
            vram_gb=$(( vram_bytes / 1024 / 1024 / 1024 ))
            # AMD vendor (0x1002) + >60 GB unified pool = Strix Halo APU
            if [[ "$vendor" == "0x1002" && "$vram_gb" -gt 60 ]]; then
                IS_STRIX_HALO=true
                USE_IMAGE="docker.io/kyuz0/amd-strix-halo-toolboxes:vulkan-radv"
                # Strix Halo has ~31 GB system RAM shared with the OS.
                # Limit parallelism and disable prompt cache to prevent OOM kills.
                EXTRA_SERVE_FLAGS="--jinja --parallel 1 --cache-ram 0"
                return
            fi
        fi
    done
}

# Strix Halo has ~113GB GPU pool but only ~31GB system RAM. KV cache lives on
# GPU (fine), but host-side compute buffers and the process RSS live in system
# RAM. Larger models leave less headroom for KV even on GPU, so we scale down.
#   ~2B  / ~4B  → full 262K (model tiny, lots of room)
#   ~7B  / ~8B  → 128K
#   ~14B / ~13B → 64K
#   ~27B / ~31B / ~34B → 32K  (e.g. Gemma 4 31B: 18GB model + ~4GB KV = 22GB)
#   ~70B / ~72B → 32K  (model alone ~40GB, leave room for KV)
#   ~122B / MoE giants → 16K  (model ~72GB, tight even on GPU)
# Patterns use [-/_] separators to avoid e.g. *1B* matching "31B".
# ---------------------------------------------------------------------------
get_ctx_size() {
    local model="$1"
    case "$model" in
        # MoE / very large — check active-param tags first
        *A10B*|*A14B*|*A4B*|*122B*|*123B*|*110B*)           echo  16384 ;;
        # ~70B dense
        *[-/_]70B*|*[-/_]72B*|*[-/_]65B*)                   echo  32768 ;;
        # ~27–34B dense (includes Gemma 4 31B)
        *[-/_]27B*|*[-/_]30B*|*[-/_]31B*|*[-/_]32B*|*[-/_]34B*)  echo  32768 ;;
        # ~12–14B
        *[-/_]12B*|*[-/_]13B*|*[-/_]14B*)                   echo  65536 ;;
        # ~7–8B and small multimodal (E4B)
        *[-/_]7B*|*[-/_]8B*|*E4B*|*[-/_]4B*)                echo 131072 ;;
        # ≤3B and small multimodal (E2B)
        *E2B*|*[-/_]1B*|*[-/_]2B*|*[-/_]3B*)                echo 262144 ;;
        # Unknown — conservative default
        *)                                                    echo  32768 ;;
    esac
}

# ============================================================================
# SHARED HELPERS
# ============================================================================

# Echo action but skip execution in dry-run mode
dry_run_cmd() {
    if $DRY_RUN; then
        echo "[dry-run] $*"
    else
        eval "$@"
    fi
}

# Run commands locally or via SSH
run_cmd() {
    if [ -n "$REMOTE_HOST" ]; then
        ssh -o ConnectTimeout=5 "$REMOTE_HOST" "$@"
    else
        bash -c "$@"
    fi
}

# Get models in JSON format.
# When REMOTE_ENDPOINT is set, fetch from /v1/models HTTP API instead of
# running ramalama locally — avoids needing ramalama/podman on the client.
get_models_json() {
    if [ -n "$REMOTE_ENDPOINT" ]; then
        curl -s --max-time 5 "${REMOTE_ENDPOINT}/v1/models" \
            | jq '[.data[] | {name: .id, size: 0}]'
    else
        run_cmd "ramalama list --json"
    fi
}

# Check if ramalama is running with the correct image.
# For the default model, also considers the quadlet systemd service.
is_ramalama_running() {
    # Remote endpoint: check liveness via HTTP health endpoint.
    if [ -n "$REMOTE_ENDPOINT" ]; then
        curl -sf "${REMOTE_ENDPOINT}/health" > /dev/null 2>&1
        return
    fi
    if [[ "$SELECTED_MODEL" == "$DEFAULT_MODEL" ]]; then
        systemctl --user is-active --quiet "$QUADLET_SERVICE" && return 0
    fi
    run_cmd "ramalama ps --noheading" | grep -q "${CONTAINER_NAME}"
}

# ---------------------------------------------------------------------------
# detect_gtt: read GTT total and available from sysfs (Strix Halo unified pool)
# Sets GTT_TOTAL_GB and GTT_AVAIL_GB globals.
# ---------------------------------------------------------------------------
detect_gtt() {
    GTT_TOTAL_GB=0
    GTT_AVAIL_GB=0
    for card in /sys/class/drm/card*/device; do
        # Use mem_info_vram_total — reflects the BIOS VRAM allocation (e.g. 96GB on Strix Halo).
        # mem_info_gtt_total only shows the GTT aperture (~15GB), NOT the full GPU pool.
        VRAM_TOTAL_PATH="${card}/mem_info_vram_total"
        VRAM_USED_PATH="${card}/mem_info_vram_used"
        if [[ -f "$VRAM_TOTAL_PATH" && -f "$VRAM_USED_PATH" ]]; then
            TOTAL_VRAM_BYTES=$(cat "$VRAM_TOTAL_PATH")
            USED_VRAM_BYTES=$(cat "$VRAM_USED_PATH")
            GTT_TOTAL_GB=$(( TOTAL_VRAM_BYTES / 1024 / 1024 / 1024 ))
            GTT_AVAIL_GB=$(( (TOTAL_VRAM_BYTES - USED_VRAM_BYTES) / 1024 / 1024 / 1024 ))
            return
        fi
    done
    # Fallback: use system RAM
    GTT_TOTAL_GB=$(free -g | awk '/^Mem:/{print $2}')
    GTT_AVAIL_GB=$(free -g | awk '/^Mem:/{print $7}')
}

# ---------------------------------------------------------------------------
# get_ramalama_endpoint: Returns the base endpoint URL, detecting actual port
# ---------------------------------------------------------------------------
get_ramalama_endpoint() {
    # Direct remote endpoint — use as-is.
    if [ -n "$REMOTE_ENDPOINT" ]; then
        echo "$REMOTE_ENDPOINT"
        return
    fi

    local host_base="http://${LOCAL_HOST}"
    local port="${LOCAL_PORT}"

    # If a container named $CONTAINER_NAME exists, try to get its mapped port
    if run_cmd "podman inspect --format='{{.State.Status}}' ${CONTAINER_NAME} 2>/dev/null" | grep -q 'running'; then
        local mapped_port
        mapped_port=$(run_cmd "podman port ${CONTAINER_NAME} 2>/dev/null" | grep -o ':[0-9]\+$' | head -n1 | tr -d ':')
        if [ -n "$mapped_port" ]; then
            port="$mapped_port"
        fi
    fi

    echo "${host_base}:${port}"
}

# ---------------------------------------------------------------------------
# check_resources: warn if available unified memory is less than required
# ---------------------------------------------------------------------------
check_resources() {
    local required_gb=$1
    [ -n "$REMOTE_HOST" ] && return     # skip for SSH remote
    [ -n "$REMOTE_ENDPOINT" ] && return # skip for direct HTTP remote

    detect_gtt
    local AVAIL_GTT=$GTT_AVAIL_GB

    if [ "$AVAIL_GTT" -lt "$required_gb" ]; then
        gum style --border normal --margin "1" --padding "1" --border-foreground 212 \
            "⚠️  LOW MEMORY DETECTED (Strix Halo Unified Pool)" \
            "Available: ${AVAIL_GTT}GB  |  Required for Model: ${required_gb}GB"

        if gum confirm "Do you want to kill some processes to free up RAM?"; then
            SELECTION=$(ps axo pid,pmem,comm --sort=-pmem | head -n 21 | tail -n +2 | \
                awk '{printf "%-7s | %-5s%% | %s\n", $1, $2, $3}' | \
                gum choose --header "Select processes to kill (Esc to skip)" --no-limit)

            if [ -n "$SELECTION" ]; then
                PIDS=$(echo "$SELECTION" | awk '{print $1}')
                for PID in $PIDS; do
                    gum spin --spinner dot --title "Killing process $PID..." -- sleep 0.5
                    kill -9 "$PID" 2>/dev/null && echo "✅ Killed $PID" || echo "❌ Failed to kill $PID"
                done
                check_resources "$required_gb"
            fi
        fi
    fi
}

# ---------------------------------------------------------------------------
# wait_for_server: poll until the /v1/models endpoint responds or timeout
# ---------------------------------------------------------------------------
wait_for_server() {
    local endpoint="${1}/v1/models"
    local max_wait=120
    local count=0

    gum style --foreground 212 "⏳ Waiting for RamaLama server at ${endpoint}..."

    while true; do
        HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${endpoint}" 2>/dev/null)
        if [[ "$HTTP_CODE" == "200" ]]; then
            gum style --foreground 121 "✅ RamaLama server is ready! (${count}s)"
            gum style --foreground 39 \
                "   🌐 Web UI → ${1}" \
                "   🔌 API    → ${1}/v1"
            return 0
        fi
        sleep 1
        (( count++ ))
        if [ "$count" -ge "$max_wait" ]; then
            gum style --foreground 196 "❌ RamaLama server did not respond after ${max_wait}s (last HTTP ${HTTP_CODE})."
            gum style --foreground 196 "   Check container logs: ramalama logs ${CONTAINER_NAME}"
            return 1
        fi
        # Show progress every 10 seconds
        if (( count % 10 == 0 )); then
            gum style --foreground 212 "   Still waiting... (${count}s / ${max_wait}s, HTTP ${HTTP_CODE})"
        fi
    done
}

# ---------------------------------------------------------------------------
# get_model_id_from_endpoint: derive a clean model id from the running server
# ---------------------------------------------------------------------------
get_model_id_from_endpoint() {
    local endpoint="${1}/v1/models"
    curl -s --max-time 5 "${endpoint}" | jq -r '.data[0].id // empty' | \
        sed 's|^/mnt/models/||; s|\.gguf$||'
}

# ---------------------------------------------------------------------------
# configure_opencode: write the ramalama provider + model into opencode.json
# The opencode openai-compatible provider expects:
#   provider.<id>.models.<model-key> = { name: "..." }
#   model = "<provider-id>/<model-key>"
# The model-key must be exactly the model ID the server returns from /v1/models
# ---------------------------------------------------------------------------
configure_opencode() {
    local base_endpoint="$1"
    local model_id="$2"        # e.g. hf://unsloth/Qwen3-Coder-Next-GGUF:Q4_K_M
    local model_display="$3"   # human-friendly name

    [ -f "$OPENCODE_CONFIG" ] || return

    local base_url="${base_endpoint}/v1"
    # Legacy SSH remote: override with explicit host:port
    [ -n "$REMOTE_HOST" ] && base_url="http://${REMOTE_HOST}:${LOCAL_PORT}/v1"

    TEMP=$(mktemp --suffix=.json)

    jq \
        --arg key    "$model_id" \
        --arg name   "$model_display" \
        --arg base   "$base_url" \
        --arg full   "ramalama/${model_id}" \
        --argjson ctx "$CTX_SIZE" \
        '
        .model = $full |
        .provider.ramalama = {
          "name": "RamaLama (llama.cpp)",
          "npm":  "@ai-sdk/openai-compatible",
          "options": { "baseURL": $base },
          "models": {
            ($key): {
              "_launch": true,
              "name": $name,
              "limit": { "context": $ctx, "output": 8192 }
            }
          }
        }
        ' \
        "$OPENCODE_CONFIG" > "$TEMP" && mv "$TEMP" "$OPENCODE_CONFIG"

    gum style --foreground 121 "📝 OpenCode configured → provider: ramalama / model: ${model_id}"
}

# ---------------------------------------------------------------------------
# _llmfit_resolve_name <ramalama_model>
# Converts a ramalama model name (hf://provider/Name-GGUF:Q4_K_M) to llmfit
# JSON info by trying progressively looser name matches.
# Prints JSON to stdout; returns 1 on failure.
# ---------------------------------------------------------------------------
_llmfit_resolve_name() {
    local model="$1"
    command -v llmfit &>/dev/null || return 1

    # Strip hf:// prefix and :QUANT suffix → e.g. "unsloth/gemma-4-31B-it-GGUF"
    local stripped
    stripped=$(echo "$model" | sed 's|hf://||; s|:[^/]*$||')

    local base="${stripped##*/}"
    local info
    for candidate in \
            "${base%-GGUF}" \
            "$base" \
            "${stripped%-GGUF}" \
            "$stripped" ; do
        [ -z "$candidate" ] && continue
        info=$(llmfit info "$candidate" --json 2>/dev/null)
        if echo "$info" | jq -e '.models[0]' >/dev/null 2>&1; then
            echo "$info"
            return 0
        fi
    done
    return 1
}

# ---------------------------------------------------------------------------
# _show_llmfit_panel <ramalama_model>
# Fetches llmfit info and renders a rich gum panel.
# ---------------------------------------------------------------------------
_show_llmfit_panel() {
    local model="$1"
    command -v llmfit &>/dev/null || { echo "ℹ️  Install llmfit for model info"; return; }

    local info
    info=$(_llmfit_resolve_name "$model") || {
        gum style --foreground 245 "ℹ️  No llmfit data found for ${model##*/}"
        return
    }

    local m; m=$(echo "$info" | jq '.models[0]')

    local name provider params use_case category license released
    name=$(     echo "$m" | jq -r '.name              // "—"')
    provider=$( echo "$m" | jq -r '.provider          // "—"')
    params=$(   echo "$m" | jq -r '.parameter_count   // "—"')
    use_case=$( echo "$m" | jq -r '.use_case          // "—"')
    category=$( echo "$m" | jq -r '.category          // "—"')
    license=$(  echo "$m" | jq -r '.license           // "Unknown"')
    released=$( echo "$m" | jq -r '.release_date      // "Unknown"')

    local fit run_mode tps mem_gb mem_pct ctx best_quant score
    fit=$(       echo "$m" | jq -r '.fit_level         // "—"')
    run_mode=$(  echo "$m" | jq -r '.run_mode          // "—"')
    tps=$(       echo "$m" | jq -r '.estimated_tps     // "—"')
    mem_gb=$(    echo "$m" | jq -r '.memory_required_gb // "—"')
    mem_pct=$(   echo "$m" | jq -r '.utilization_pct   // "—"')
    ctx=$(       echo "$m" | jq -r '.context_length    // "—"')
    best_quant=$(echo "$m" | jq -r '.best_quant        // "—"')
    score=$(     echo "$m" | jq -r '.score             // "—"')

    local q_quality q_speed q_fit q_ctx
    q_quality=$(echo "$m" | jq -r '.score_components.quality // "—"')
    q_speed=$(  echo "$m" | jq -r '.score_components.speed   // "—"')
    q_fit=$(    echo "$m" | jq -r '.score_components.fit     // "—"')
    q_ctx=$(    echo "$m" | jq -r '.score_components.context // "—"')

    # Fit colour: green=perfect, yellow=tight, red=too large
    local fit_sym="🟢"; [[ "$fit" == *"Tight"* ]] && fit_sym="🟡"; [[ "$fit" == *"Too"* ]] && fit_sym="🔴"

    # GGUF sources (up to 3) and notes (up to 4)
    local gguf_lines notes_lines
    mapfile -t gguf_lines < <(echo "$m" | jq -r '.gguf_sources[]? | "  • \(.provider) → https://huggingface.co/\(.repo)"' 2>/dev/null | head -3)
    mapfile -t notes_lines < <(echo "$m" | jq -r '.notes[]?' 2>/dev/null | sed 's/^/  • /' | head -4)

    local lines=(
        "$(gum style --bold --foreground 33 "${name}")"
        ""
        "$(printf "  %-16s %s" "Provider:"    "$provider")"
        "$(printf "  %-16s %s" "Parameters:"  "$params")"
        "$(printf "  %-16s %s" "Category:"    "$category")"
        "$(printf "  %-16s %s" "Use case:"    "$use_case")"
        "$(printf "  %-16s %s" "License:"     "$license")"
        "$(printf "  %-16s %s" "Released:"    "$released")"
        ""
        "$(gum style --bold "  Hardware fit on this system:")"
        "$(printf "  %-16s %s %s" "Fit:"       "$fit_sym" "$fit")"
        "$(printf "  %-16s %s" "Run mode:"    "$run_mode")"
        "$(printf "  %-16s %s tok/s" "Speed est.:" "$tps")"
        "$(printf "  %-16s %s GB  (%s%%)" "VRAM needed:" "$mem_gb" "$mem_pct")"
        "$(printf "  %-16s %s tokens" "Context:"    "$ctx")"
        "$(printf "  %-16s %s  (best for hw: %s)" "Quant:" "—" "$best_quant")"
        ""
        "$(gum style --bold "  Score: ${score}/100")"
        "  Quality: ${q_quality}   Speed: ${q_speed}   Fit: ${q_fit}   Context: ${q_ctx}"
    )
    if [ ${#gguf_lines[@]} -gt 0 ]; then
        lines+=("" "$(gum style --bold "  GGUF Sources:")" "${gguf_lines[@]}")
    fi
    if [ ${#notes_lines[@]} -gt 0 ]; then
        lines+=("" "$(gum style --bold "  Notes:")" "${notes_lines[@]}")
    fi

    gum style --border rounded --padding "1 3" --border-foreground 33 "${lines[@]}"
}

# ---------------------------------------------------------------------------
# pick_model HEADER [models_json]
# Prints selected model name to stdout; returns 1 if user cancels.
# fzf mode (preferred): press i to show llmfit info panel, enter to select.
# gum fallback: select then navigate a Use / Info / Back sub-menu.
# ---------------------------------------------------------------------------
pick_model() {
    local header="$1"
    local models_json="${2:-$(get_models_json)}"

    local models_with_size default_line other_lines
    models_with_size=$(echo "$models_json" | jq -r '.[] | "\(.name)  (\((.size//0)/1073741824|floor)GB)"')
    default_line=$(echo "$models_with_size" | grep -F "$DEFAULT_MODEL" | head -1)
    if [ -n "$default_line" ]; then
        other_lines=$(echo "$models_with_size" | grep -vF "$DEFAULT_MODEL")
        models_with_size=$(printf "%s\n%s" "$default_line" "$other_lines")
    fi

    if command -v fzf &>/dev/null && command -v llmfit &>/dev/null; then
        # --- fzf path: 'i' triggers info panel without leaving the picker -------
        local info_script; info_script=$(mktemp /tmp/oramalama-info-XXXXXX.sh)
        chmod +x "$info_script"
        cat > "$info_script" << 'INFOSCRIPT'
#!/usr/bin/env bash
model="$(echo "$1" | sed 's/  (.*//')"
base="${model##*/}"; base="${base%:*}"; base="${base%-GGUF}"
info=$(llmfit info "$base" --json 2>/dev/null)
if ! echo "$info" | jq -e '.models[0]' >/dev/null 2>&1; then
    gum style --foreground 245 "ℹ️  No llmfit data found for: $base"
    printf "\n"; read -rsp "Press any key..." -n1; echo; exit
fi
m=$(echo "$info" | jq '.models[0]')
name=$(  echo "$m" | jq -r '.name              // "—"')
prov=$(  echo "$m" | jq -r '.provider          // "—"')
params=$(echo "$m" | jq -r '.parameter_count   // "—"')
use=$(   echo "$m" | jq -r '.use_case          // "—"')
fit=$(   echo "$m" | jq -r '.fit_level         // "—"')
run=$(   echo "$m" | jq -r '.run_mode          // "—"')
tps=$(   echo "$m" | jq -r '.estimated_tps     // "—"')
mem=$(   echo "$m" | jq -r '.memory_required_gb // "—"')
pct=$(   echo "$m" | jq -r '.utilization_pct   // "—"')
sc=$(    echo "$m" | jq -r '.score             // "—"')
sq=$(    echo "$m" | jq -r '.score_components.quality // "—"')
ss=$(    echo "$m" | jq -r '.score_components.speed   // "—"')
sf=$(    echo "$m" | jq -r '.score_components.fit     // "—"')
fit_sym="🟢"; [[ "$fit" == *"Tight"* ]] && fit_sym="🟡"; [[ "$fit" == *"Too"* ]] && fit_sym="🔴"
gum style --border rounded --padding "1 3" --border-foreground 33 \
    "$(gum style --bold --foreground 33 "$name")" \
    "" \
    "$(printf "  %-16s %s" "Provider:"   "$prov")" \
    "$(printf "  %-16s %s" "Parameters:" "$params")" \
    "$(printf "  %-16s %s" "Use case:"   "$use")" \
    "" \
    "$(gum style --bold "  Hardware fit:")" \
    "$(printf "  %-16s %s %s" "Fit:"     "$fit_sym" "$fit")" \
    "$(printf "  %-16s %s" "Run mode:"   "$run")" \
    "$(printf "  %-16s %s tok/s" "Speed:" "$tps")" \
    "$(printf "  %-16s %s GB  (%s%%)" "VRAM:" "$mem" "$pct")" \
    "$(printf "  %-16s %s/100  (quality %s · speed %s · fit %s)" "Score:" "$sc" "$sq" "$ss" "$sf")"
printf "\n"; read -rsp "Press any key to return to model list..." -n1; echo
INFOSCRIPT

        local selected_line
        selected_line=$(echo "$models_with_size" \
            | fzf --no-sort --reverse \
                  --header "$header  [i = model info]" \
                  --bind "i:execute($info_script {})")
        local rc=$?
        rm -f "$info_script"
        [ $rc -ne 0 ] && return 1
        echo "$selected_line" | sed 's/  (.*//'
        return 0
    fi

    # --- gum fallback: sub-menu after selection --------------------------------
    while true; do
        local selected_line
        selected_line=$(echo "$models_with_size" | gum choose --header "$header  [enter=select]") || return 1
        [ -z "$selected_line" ] && return 1
        local selected_model
        selected_model=$(echo "$selected_line" | sed 's/  (.*//')

        if command -v llmfit &>/dev/null; then
            local short_name action=""
            short_name=$(echo "$selected_model" | sed 's|hf://[^/]*/||; s|:.*||')
            while [[ "$action" != "▶"* ]]; do
                [[ "$action" == "ℹ️"* ]] && _show_llmfit_panel "$selected_model"
                action=$(printf "▶  Use: %s\nℹ️  Model info (llmfit)\n←  Back to model list\n" \
                                "$short_name" \
                         | gum choose --header "Selected: ${short_name}") || action="←"
                [[ "$action" == "←"* ]] && break
            done
            [[ "$action" == "←"* ]] && continue
        fi

        echo "$selected_model"
        return 0
    done
}

# ---------------------------------------------------------------------------
# ensure_server_running: start server if needed; handle model switch if already
# running. Sets globals: ENDPOINT, SELECTED_MODEL, SERVED_MODEL_ID, CTX_SIZE
# ---------------------------------------------------------------------------
ensure_server_running() {
    if [ -n "$REMOTE_ENDPOINT" ] && ! curl -sf "${REMOTE_ENDPOINT}/health" >/dev/null 2>&1; then
        gum style --foreground 196 "❌ Cannot reach ramalama at ${REMOTE_ENDPOINT} — is the server running and reachable?"
        exit 1
    fi

    if ! is_ramalama_running; then
        if [ -n "$REMOTE_ENDPOINT" ]; then
            echo "❌ Remote endpoint ${REMOTE_ENDPOINT} not healthy." && exit 1
        fi

        # No local server and no --remote: offer to scan the network first
        if ! $DRY_RUN && gum confirm \
            "No local ramalama server running. Scan network for a remote server?" \
            --default=no 2>/dev/null; then
            if cmd_discover; then
                # Discovery set REMOTE_ENDPOINT — restart ensure_server_running on the remote path
                ensure_server_running
                return
            fi
            echo "ℹ️  No remote server selected — starting local server."
        fi

        if run_cmd "ramalama ps --noheading" | grep -q "${CONTAINER_NAME}"; then
            gum style --foreground 212 "🔄 Restarting RamaLama with optimized Strix Halo toolbox..."
            run_cmd "ramalama stop ${CONTAINER_NAME}"
            sleep 2
        fi

        # Model selection
        local models_json model_names
        models_json=$(get_models_json)
        model_names=$(echo "$models_json" | jq -r '.[].name // empty')

        if [ -z "$model_names" ]; then
            echo "❌ No RamaLama models found on $([ -n "$REMOTE_HOST" ] && echo "$REMOTE_HOST" || echo "local system")."
            if command -v llmfit &>/dev/null; then
                if $DRY_RUN || gum confirm "Run llmfit to find & pull a recommended model?"; then
                    cmd_search
                fi
            fi
            exit 1
        fi

        if [ -n "$CLI_MODEL" ]; then
            if echo "$model_names" | grep -qF "$CLI_MODEL"; then
                SELECTED_MODEL="$CLI_MODEL"
            else
                echo "❌ Model not found in ramalama list: $CLI_MODEL"
                echo "Available models:"
                echo "$model_names" | sed 's/^/  /'
                exit 1
            fi
        else
            SELECTED_MODEL=$(pick_model "Select a model to serve  [default: gemma-4-31B-it Q4_K_M]" "$models_json") || exit 1
        fi

        local model_size_bytes model_size_gb required_gb
        model_size_bytes=$(echo "$models_json" | jq -r --arg n "$SELECTED_MODEL" '.[] | select(.name == $n) | .size')
        model_size_gb=$(( ${model_size_bytes:-0} / 1024 / 1024 / 1024 ))
        required_gb=$(( model_size_gb + 4 ))

        CTX_SIZE=$(get_ctx_size "$SELECTED_MODEL")
        echo "📐 Context window for ${SELECTED_MODEL}: ${CTX_SIZE} tokens"

        detect_gtt

        if [ "$model_size_gb" -gt "$GTT_TOTAL_GB" ] && [ "$GTT_TOTAL_GB" -gt 0 ]; then
            gum style --border double --border-foreground 196 --margin "1" --padding "1" \
                "🚫 MODEL TOO LARGE FOR GPU POOL" \
                "Model size:       ${model_size_gb}GB" \
                "GPU pool (GTT):   ${GTT_TOTAL_GB}GB  (${GTT_AVAIL_GB}GB free)" \
                "This model will NOT load. Select a smaller model or increase VRAM in BIOS."

            if command -v llmfit &>/dev/null && gum confirm "Show llmfit-recommended models that DO fit?"; then
                cmd_search
            fi
            exit 1
        fi

        check_resources "$required_gb"

        echo "�� Starting RamaLama with ${SELECTED_MODEL}..."
        if [[ "$SELECTED_MODEL" == "$DEFAULT_MODEL" ]] && systemctl --user list-unit-files --quiet "$QUADLET_SERVICE" &>/dev/null; then
            dry_run_cmd "systemctl --user start '${QUADLET_SERVICE}'"
        else
            local image_flag=""
            [ -n "$USE_IMAGE" ] && image_flag="--image ${USE_IMAGE}"
            dry_run_cmd "run_cmd 'ramalama serve --detach --name ${CONTAINER_NAME} ${image_flag} -c ${CTX_SIZE} --runtime-args=\"${EXTRA_SERVE_FLAGS}\" ${SELECTED_MODEL}'"
        fi

        sleep 2
        ENDPOINT=$(get_ramalama_endpoint)

        if [ -n "$REMOTE_HOST" ]; then
            local remote_port
            remote_port=$(echo "$ENDPOINT" | grep -o ':[0-9]\+$' | tr -d ':')
            echo "ℹ️  Ensure port ${remote_port} is accessible from ${REMOTE_HOST}"
            ENDPOINT="http://${REMOTE_HOST}:${remote_port}"
        fi

        if ! $DRY_RUN && ! wait_for_server "$ENDPOINT"; then
            echo "❌ Aborting — server failed to start."
            exit 1
        fi

        if $DRY_RUN; then
            SERVED_MODEL_ID="$SELECTED_MODEL"
        else
            SERVED_MODEL_ID=$(get_model_id_from_endpoint "$ENDPOINT")
            [ -z "$SERVED_MODEL_ID" ] && SERVED_MODEL_ID="$SELECTED_MODEL"
        fi
    else
        # Already running — detect the live model then decide whether to switch.
        ENDPOINT=$(get_ramalama_endpoint)

        if ! $DRY_RUN; then
            local running_model
            running_model=$(get_model_id_from_endpoint "$ENDPOINT")
            if [ -z "$running_model" ]; then
                local container_model
                container_model=$(podman inspect --format='{{.Args}}' ${CONTAINER_NAME} 2>/dev/null | grep -o 'hf://[^ ]*' | tr -d ']')
                running_model="${container_model:-unknown-model}"
            fi
            echo "✅ RamaLama already running with: ${running_model} on ${ENDPOINT}"

            # Determine desired model (flag or interactive picker)
            local desired_model
            if [ -n "$CLI_MODEL" ]; then
                desired_model="$CLI_MODEL"
            else
                local models_json
                models_json=$(get_models_json)
                desired_model=$(pick_model "Running: ${running_model} — keep or switch?  [default: gemma-4-31B-it Q4_K_M]" "$models_json") || exit 0
            fi

            if [[ "$desired_model" != "$running_model" ]]; then
                gum style --foreground 212 "🔄 Switching model: ${running_model} → ${desired_model}"
                if systemctl --user is-active --quiet "$QUADLET_SERVICE" 2>/dev/null; then
                    dry_run_cmd "systemctl --user stop '${QUADLET_SERVICE}'"
                else
                    dry_run_cmd "ramalama stop '${CONTAINER_NAME}'"
                fi
                sleep 2

                SELECTED_MODEL="$desired_model"
                CTX_SIZE=$(get_ctx_size "$SELECTED_MODEL")
                echo "📐 Context window for ${SELECTED_MODEL}: ${CTX_SIZE} tokens"
                detect_gtt

                local switch_models_json switch_size_bytes switch_size_gb switch_required_gb
                switch_models_json=$(get_models_json)
                switch_size_bytes=$(echo "$switch_models_json" | jq -r --arg n "$SELECTED_MODEL" '.[] | select(.name == $n) | .size // 0')
                switch_size_gb=$(( ${switch_size_bytes:-0} / 1024 / 1024 / 1024 ))
                switch_required_gb=$(( switch_size_gb + 4 ))
                check_resources "$switch_required_gb"

                echo "🚀 Starting RamaLama with ${SELECTED_MODEL}..."
                if [[ "$SELECTED_MODEL" == "$DEFAULT_MODEL" ]] && systemctl --user list-unit-files --quiet "$QUADLET_SERVICE" &>/dev/null; then
                    dry_run_cmd "systemctl --user start '${QUADLET_SERVICE}'"
                else
                    local image_flag=""
                    [ -n "$USE_IMAGE" ] && image_flag="--image ${USE_IMAGE}"
                    dry_run_cmd "run_cmd 'ramalama serve --detach --name ${CONTAINER_NAME} ${image_flag} -c ${CTX_SIZE} --runtime-args=\"${EXTRA_SERVE_FLAGS}\" ${SELECTED_MODEL}'"
                fi
                sleep 2
                ENDPOINT=$(get_ramalama_endpoint)
                if ! $DRY_RUN && ! wait_for_server "$ENDPOINT"; then
                    echo "❌ Aborting — server failed to start after switch."
                    exit 1
                fi
            else
                SELECTED_MODEL="$running_model"
            fi

            SERVED_MODEL_ID=$(get_model_id_from_endpoint "$ENDPOINT")
            [ -z "$SERVED_MODEL_ID" ] && SERVED_MODEL_ID="$SELECTED_MODEL"
        else
            SERVED_MODEL_ID="${CLI_MODEL:-dry-run-model}"
            SELECTED_MODEL="$SERVED_MODEL_ID"
        fi
        CTX_SIZE=$(get_ctx_size "$SELECTED_MODEL")
    fi
}

# ============================================================================
# SUBCOMMAND FUNCTIONS
# ============================================================================

cmd_list() {
    local models_json
    models_json=$(get_models_json)
    if [ -z "$models_json" ] || [ "$models_json" = "[]" ]; then
        echo "No models found. Run: oramalama pull"
        return
    fi
    local header="NAME\tSIZE\tCONTEXT\tMODIFIED"
    local rows
    rows=$(echo "$models_json" | jq -r '.[] |
        [
            .name,
            ((.size // 0) / 1073741824 | floor | tostring) + "GB",
            "",
            (.modified // "—")
        ] | @tsv')
    local table_data="$header"
    while IFS=$'\t' read -r name size _ctx modified; do
        local ctx
        ctx=$(get_ctx_size "$name")
        local display_name="$name"
        [[ "$name" == "$DEFAULT_MODEL" ]] && display_name="★ $name"
        table_data+=$'\n'"${display_name}\t${size}\t${ctx}\t${modified}"
    done <<< "$rows"
    echo "$table_data" | gum table
}

cmd_ps() {
    if [ -n "$REMOTE_ENDPOINT" ]; then
        if curl -sf "${REMOTE_ENDPOINT}/health" >/dev/null 2>&1; then
            local model
            model=$(curl -s "${REMOTE_ENDPOINT}/v1/models" | jq -r '[.data[].id] | join(", ")' 2>/dev/null)
            echo "NAME\tENDPOINT\tSTATUS" | gum table
            printf "%s\t%s\t%s\n" "$model" "$REMOTE_ENDPOINT" "running" | gum table
        else
            echo "No server running at ${REMOTE_ENDPOINT}"
        fi
        return
    fi
    local ps_out
    ps_out=$(ramalama ps --noheading 2>/dev/null)
    if [ -z "$ps_out" ]; then
        echo "No models currently running."
        return
    fi
    local endpoint
    endpoint=$(get_ramalama_endpoint)
    echo "NAME\tMODEL\tENDPOINT" | gum table
    echo "$ps_out" | while read -r line; do
        local cname model
        cname=$(echo "$line" | awk '{print $NF}')
        model=$(podman inspect --format='{{index .Config.Cmd}}' "$cname" 2>/dev/null | grep -o 'hf://[^ ]*\|/mnt/models/[^ ]*' | head -1)
        printf "%s\t%s\t%s\n" "$cname" "${model:-unknown}" "$endpoint"
    done | gum table
}

cmd_pull() {
    local model="${1:-}"
    if [ -z "$model" ] && [ -n "$CLI_MODEL" ]; then
        model="$CLI_MODEL"
    fi
    if [ -z "$model" ]; then
        cmd_search
        return
    fi
    echo "📥 Pulling ${model}..."
    if $DRY_RUN; then
        echo "[dry-run] ramalama pull ${model}"
        return
    fi
    ramalama pull "$model" && \
        gum style --foreground 121 "✅ Pulled ${model}" || \
        gum style --foreground 196 "❌ Pull failed"
}

cmd_rm() {
    local model="${1:-$CLI_MODEL}"
    if [ -z "$model" ]; then
        local models_json
        models_json=$(get_models_json)
        local selected
        selected=$(echo "$models_json" | jq -r '.[] | "\(.name)  (\((.size//0)/1073741824|floor)GB)"' \
            | gum choose --header "Select model to remove")
        [ -z "$selected" ] && return
        model=$(echo "$selected" | sed 's/  (.*//')
    fi
    gum confirm "Remove ${model}?" || return
    dry_run_cmd "ramalama rm '${model}'" && \
        gum style --foreground 121 "✅ Removed ${model}"
}

# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# SSH helpers for remote model management
# ---------------------------------------------------------------------------

# _resolve_ssh_host <ip>: find a working SSH hostname for an IP.
# Checks Tailscale MagicDNS (short name then full), then raw IP.
# Prints the hostname on success; prints nothing on failure.
_resolve_ssh_host() {
    local ip="$1"
    local candidates=()

    # Try Tailscale MagicDNS lookup
    if command -v tailscale &>/dev/null; then
        local dns
        dns=$(tailscale status --json 2>/dev/null \
            | jq -r --arg ip "$ip" \
              '.Peer[] | select(.TailscaleIPs[] == $ip) | .DNSName' \
              2>/dev/null | head -1 | sed 's/\.$//')
        [ -n "$dns" ] && candidates+=("${dns%%.*}" "$dns")
    fi
    candidates+=("$ip")

    for host in "${candidates[@]}"; do
        if ssh -o ConnectTimeout=3 -o BatchMode=yes \
               -o StrictHostKeyChecking=no "$host" \
               bash -l -c true 2>/dev/null; then
            echo "$host"
            return 0
        fi
    done
}

# _remote_list_models <ssh_host>: list models available on a remote machine.
# Prints a JSON array compatible with get_models_json output.
_remote_list_models() {
    local host="$1"
    ssh -o ConnectTimeout=5 "$host" bash -l -c \
        "'command -v oramalama &>/dev/null \
             && oramalama list --json 2>/dev/null \
             || ramalama list --json 2>/dev/null'" 2>/dev/null
}

# _remote_switch_model <ssh_host> <model>: stop the current server on the
# remote and start a new one. Polls REMOTE_ENDPOINT until healthy again.
# Updates SERVED_MODEL_ID global on success.
_remote_switch_model() {
    local host="$1"
    local model="$2"

    gum style --foreground 212 "🔄 Switching remote model → ${model}..."

    # Prefer oramalama stop for a clean shutdown; fall back to raw ramalama
    if ssh -o ConnectTimeout=5 "$host" bash -l -c \
           "'command -v oramalama &>/dev/null'" 2>/dev/null; then
        ssh "$host" bash -l -c \
            "'oramalama stop 2>/dev/null || true
              sleep 2
              ramalama serve --detach --name ramalama-opencode \"${model}\" 2>&1'" &
    else
        ssh "$host" bash -l -c \
            "'ramalama ps --noheading 2>/dev/null \
                | awk \"{print \\\$1}\" \
                | while read -r c; do ramalama stop \"\$c\" 2>/dev/null || true; done
              systemctl --user stop ramalama-opencode.service 2>/dev/null || true
              sleep 2
              ramalama serve --detach --name ramalama-opencode \"${model}\" 2>&1'" &
    fi
    local bg_pid=$!

    # Poll until the remote server is healthy again (max 90 s)
    local spin='⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏' spin_i=0 waited=0
    while [ $waited -lt 90 ]; do
        sleep 2; (( waited += 2 )) || true
        if curl -sf --max-time 2 "${REMOTE_ENDPOINT}/health" >/dev/null 2>&1; then
            printf "\r                                         \r"
            kill "$bg_pid" 2>/dev/null; wait "$bg_pid" 2>/dev/null
            local live_model
            live_model=$(curl -s --max-time 3 "${REMOTE_ENDPOINT}/v1/models" \
                | jq -r '.data[0].id' 2>/dev/null)
            SERVED_MODEL_ID="${live_model:-$model}"
            gum style --foreground 121 "✅ Remote model → ${SERVED_MODEL_ID}"
            return 0
        fi
        printf "\r  %s  Waiting for remote server... %ds" \
               "${spin:$((spin_i % ${#spin})):1}" "$waited"
        (( spin_i++ )) || true
    done
    printf "\r                                          \r"
    kill "$bg_pid" 2>/dev/null; wait "$bg_pid" 2>/dev/null
    gum style --foreground 196 "❌ Timed out waiting for remote server to come back."
    return 1
}

# cmd_discover: scan Tailscale peers + local /24 subnet for ramalama servers.
# On success: sets REMOTE_ENDPOINT global and returns 0.
# On cancel/no results: returns 1.
# When called as a standalone subcommand (standalone=true), drops into cmd_run.
# ---------------------------------------------------------------------------
cmd_discover() {
    local standalone="${1:-false}"

    local results_file
    results_file=$(mktemp /tmp/oramalama-discover.XXXXXX)

    gum style --bold "🔍 Scanning for ramalama servers..."
    printf "   Ports: %s\n" "${DISCOVER_PORTS[*]}"

    # ── Tailscale peers ──────────────────────────────────────────────────────
    if command -v tailscale &>/dev/null; then
        local ts_peers
        ts_peers=$(tailscale status --json 2>/dev/null \
            | jq -r '.Peer[] | select(.Online==true) | .TailscaleIPs[0]' 2>/dev/null || true)
        while IFS= read -r ip; do
            [ -z "$ip" ] && continue
            for port in "${DISCOVER_PORTS[@]}"; do
                (
                    if curl -sf --max-time 2 "http://${ip}:${port}/health" >/dev/null 2>&1 || \
                       curl -sf --max-time 2 "http://${ip}:${port}/v1/models" >/dev/null 2>&1; then
                        echo "http://${ip}:${port}" >> "$results_file"
                    fi
                ) &
            done
        done <<< "$ts_peers"
    fi

    # ── Local /24 subnet ────────────────────────────────────────────────────
    local local_ip subnet
    local_ip=$(ip -4 route get 1.1.1.1 2>/dev/null \
        | awk '{for(i=1;i<=NF;i++) if ($i=="src") {print $(i+1); exit}}')

    if [ -n "$local_ip" ]; then
        subnet=$(echo "$local_ip" | cut -d. -f1-3)

        if command -v nmap &>/dev/null; then
            # nmap: fast open-port detection, then confirm with curl
            local ports_str
            ports_str=$(IFS=,; echo "${DISCOVER_PORTS[*]}")
            while IFS= read -r ip; do
                [ -z "$ip" ] && continue
                for port in "${DISCOVER_PORTS[@]}"; do
                    (
                        if curl -sf --max-time 2 "http://${ip}:${port}/health" >/dev/null 2>&1 || \
                           curl -sf --max-time 2 "http://${ip}:${port}/v1/models" >/dev/null 2>&1; then
                            echo "http://${ip}:${port}" >> "$results_file"
                        fi
                    ) &
                done
            done < <(nmap -p "$ports_str" --open -T4 -oG - "${subnet}.0/24" 2>/dev/null \
                     | awk '/^Host:/{print $2}')
        else
            # Pure-curl fallback: probe all 254 hosts in parallel (2 s timeout each)
            for i in $(seq 1 254); do
                local host_ip="${subnet}.${i}"
                for port in "${DISCOVER_PORTS[@]}"; do
                    (
                        if curl -sf --max-time 2 "http://${host_ip}:${port}/health" >/dev/null 2>&1 || \
                           curl -sf --max-time 2 "http://${host_ip}:${port}/v1/models" >/dev/null 2>&1; then
                            echo "http://${host_ip}:${port}" >> "$results_file"
                        fi
                    ) &
                done
            done
        fi
    fi

    # ── Spinner while background probes finish ───────────────────────────────
    local spin='⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏'
    local spin_i=0
    while jobs -r 2>/dev/null | grep -q .; do
        printf "\r  %s  Probing..." "${spin:$((spin_i % ${#spin})):1}"
        sleep 0.1
        (( spin_i++ )) || true
    done
    printf "\r                              \r"
    wait 2>/dev/null

    # ── Collect & deduplicate ────────────────────────────────────────────────
    local endpoints=()
    if [ -s "$results_file" ]; then
        while IFS= read -r ep; do
            [ -n "$ep" ] && endpoints+=("$ep")
        done < <(sort -u "$results_file")
    fi
    rm -f "$results_file"

    if [ ${#endpoints[@]} -eq 0 ]; then
        gum style --foreground 196 "❌ No ramalama servers found on local network or Tailscale."
        return 1
    fi

    # ── Enrich + deduplicate by model fingerprint ────────────────────────────
    # The same physical server often appears on multiple IPs (LAN + Tailscale).
    # We group by sorted model list; keep the best IP per group, preferring
    # Tailscale (100.x) > LAN, and show a "(+N IP)" hint for duplicates.
    declare -A _ep_display  # endpoint -> "model1, model2"
    declare -A _ep_key      # endpoint -> sorted fingerprint for grouping
    for ep in "${endpoints[@]}"; do
        local raw
        raw=$(curl -s --max-time 3 "${ep}/v1/models" 2>/dev/null)
        _ep_display[$ep]=$(echo "$raw" | jq -r '[.data[].id] | join(", ")' 2>/dev/null)
        _ep_key[$ep]=$(echo "$raw" | jq -r '[.data[].id] | sort | join(",")' 2>/dev/null)
        [ -z "${_ep_display[$ep]}" ] && _ep_display[$ep]="?"
        # Unknown servers get a unique key so they're never wrongly merged
        [ -z "${_ep_key[$ep]}" ] && _ep_key[$ep]="unknown-${ep}"
    done

    declare -A _best    # fingerprint -> chosen (best) endpoint
    declare -A _extras  # fingerprint -> count of alternative IPs hidden
    for ep in "${endpoints[@]}"; do
        local key="${_ep_key[$ep]}"
        if [ -z "${_best[$key]+x}" ]; then
            _best[$key]="$ep"
            _extras[$key]=0
        else
            (( _extras[$key]++ )) || true
            # Prefer Tailscale (100.x.x.x) — works across all networks
            local cur="${_best[$key]}"
            if [[ "$ep" =~ ^http://100\. ]] && ! [[ "$cur" =~ ^http://100\. ]]; then
                _best[$key]="$ep"
            fi
        fi
    done

    local menu_items=()
    for key in "${!_best[@]}"; do
        local ep="${_best[$key]}"
        local label="${ep}  [${_ep_display[$ep]}]"
        local n="${_extras[$key]}"
        [ "$n" -gt 0 ] && label+="  (+${n} IP)"
        menu_items+=("$label")
    done
    # Sort for consistent ordering
    mapfile -t menu_items < <(printf '%s\n' "${menu_items[@]}" | sort)
    menu_items+=("← Cancel")

    # ── Interactive picker ───────────────────────────────────────────────────
    local chosen
    chosen=$(printf '%s\n' "${menu_items[@]}" \
        | gum choose --header "🖥️  Discovered servers — select one to connect:") || return 1
    [[ "$chosen" == "← Cancel" ]] && return 1

    # Strip the "  [models]" and "(+N IP)" annotations to get the bare URL
    REMOTE_ENDPOINT="${chosen%%  *}"
    export REMOTE_ENDPOINT
    gum style --foreground 121 "✅ Connected → ${REMOTE_ENDPOINT}"

    # ── Try to resolve SSH access for the remote machine ─────────────────────
    local remote_ip
    remote_ip=$(echo "$REMOTE_ENDPOINT" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+')
    REMOTE_SSH_HOST=""
    if [ -n "$remote_ip" ]; then
        local resolved
        printf "  🔑 Checking SSH access..."
        resolved=$(_resolve_ssh_host "$remote_ip")
        printf "\r                            \r"
        if [ -n "$resolved" ]; then
            REMOTE_SSH_HOST="$resolved"
            gum style --foreground 245 "  🔑 SSH available via: ${REMOTE_SSH_HOST}"
        fi
    fi

    # If SSH is available, offer to switch the remote model before connecting
    if [ -n "$REMOTE_SSH_HOST" ]; then
        local current_model
        current_model=$(curl -s --max-time 3 "${REMOTE_ENDPOINT}/v1/models" \
            | jq -r '.data[0].id' 2>/dev/null || echo "unknown")
        local action
        action=$(printf "Connect (current model: %s)\nSwitch model on remote...\n" \
                        "$current_model" \
                 | gum choose --header "Remote: ${REMOTE_ENDPOINT}") || true
        if [[ "$action" == "Switch model"* ]]; then
            local remote_json remote_names
            remote_json=$(_remote_list_models "$REMOTE_SSH_HOST")
            remote_names=$(echo "$remote_json" | jq -r '.[].name // empty' 2>/dev/null)
            if [ -z "$remote_names" ]; then
                gum style --foreground 196 "❌ No models found on remote (is ramalama installed?)"
            else
                local new_model
                new_model=$(echo "$remote_names" \
                    | gum choose --header "Select model to run on ${REMOTE_SSH_HOST}:") || true
                [ -n "$new_model" ] && _remote_switch_model "$REMOTE_SSH_HOST" "$new_model"
            fi
        fi
    fi

    # When invoked as `oramalama discover`, drop straight into the REPL
    [[ "$standalone" == "true" ]] && cmd_run
    return 0
}

cmd_stop() {
    if [ -n "$REMOTE_ENDPOINT" ]; then
        echo "ℹ️  Cannot stop a remote server from here. Use the remote machine."
        return
    fi

    if systemctl --user is-active --quiet "$QUADLET_SERVICE" 2>/dev/null; then
        gum confirm "Stop ${QUADLET_SERVICE}?" || return
        dry_run_cmd "systemctl --user stop '${QUADLET_SERVICE}'"
        gum style --foreground 121 "✅ Stopped ${QUADLET_SERVICE}"
        return
    fi

    local running
    running=$(ramalama ps --noheading 2>/dev/null)
    if [ -z "$running" ]; then
        echo "No models currently running."
        return
    fi

    local container
    if [ -n "${1:-}" ]; then
        container="$1"
    else
        container=$(echo "$running" | awk '{print $NF}' \
            | gum choose --header "Select container to stop")
        [ -z "$container" ] && return
    fi

    gum confirm "Stop ${container}?" || return
    dry_run_cmd "ramalama stop '${container}'"
    gum style --foreground 121 "✅ Stopped ${container}"
}

cmd_show() {
    local model="${1:-$CLI_MODEL}"

    if [ -z "$model" ]; then
        local models_json
        models_json=$(get_models_json)
        model=$(pick_model "Select a model to inspect" "$models_json") || exit 0
    fi

    local models_json size_bytes size_gb ctx quant arch tags
    models_json=$(get_models_json)
    size_bytes=$(echo "$models_json" | jq -r --arg n "$model" '.[] | select(.name == $n) | .size // 0')
    size_gb=$(( ${size_bytes:-0} / 1024 / 1024 / 1024 ))
    ctx=$(get_ctx_size "$model")

    # Quant from trailing :Qx_x or :Ix tag
    quant=$(echo "$model" | grep -oE ':[QqIi][0-9][^:/ ]*' | tr -d ':' | head -1)
    [ -z "$quant" ] && quant="—"

    # Architecture from name keywords
    if   echo "$model" | grep -qi "gemma";    then arch="Gemma"
    elif echo "$model" | grep -qi "qwen";     then arch="Qwen"
    elif echo "$model" | grep -qi "llama";    then arch="Llama"
    elif echo "$model" | grep -qi "mistral";  then arch="Mistral"
    elif echo "$model" | grep -qi "phi";      then arch="Phi"
    elif echo "$model" | grep -qi "deepseek"; then arch="DeepSeek"
    elif echo "$model" | grep -qi "falcon";   then arch="Falcon"
    elif echo "$model" | grep -qi "wizard";   then arch="WizardLM"
    else arch="—"
    fi

    [[ "$model" == "$DEFAULT_MODEL" ]] && tags=" ★ default" || tags=""

    gum style \
        --border rounded \
        --padding "1 3" \
        --border-foreground 212 \
        "$(gum style --bold --foreground 212 "$model")${tags}" \
        "" \
        "$(printf "  %-14s %s" "Architecture:"  "$arch")" \
        "$(printf "  %-14s %s" "Quantization:"  "$quant")" \
        "$(printf "  %-14s %s GB" "Size:"        "$size_gb")" \
        "$(printf "  %-14s %s tokens" "Context:"  "$ctx")"

    # Show endpoint if this model is currently being served
    if is_ramalama_running 2>/dev/null; then
        local ep running_id
        ep=$(get_ramalama_endpoint)
        running_id=$(get_model_id_from_endpoint "$ep" 2>/dev/null)
        local model_base
        model_base=$(basename "$model" | sed 's/:.*//')
        if [[ "$running_id" == *"$model_base"* ]] || [[ "$model" == *"$running_id"* ]]; then
            gum style --foreground 121 "  🟢 Running → ${ep}"
        fi
    fi

    # Append llmfit panel if available
    _show_llmfit_panel "$model"
}

cmd_run() {
    local THINK=false HIDETHINKING=false VERBOSE=false
    local model="${CLI_MODEL:-}"

    while [[ $# -gt 0 ]]; do
        case "$1" in
            --think)        THINK=true;        shift ;;
            --hidethinking) HIDETHINKING=true; shift ;;
            --verbose)      VERBOSE=true;      shift ;;
            --*)            echo "Unknown flag: $1"; usage ;;
            *)              model="$1"; shift ;;
        esac
    done

    if [ -z "$model" ]; then
        local models_json model_names
        models_json=$(get_models_json)
        model_names=$(echo "$models_json" | jq -r '.[].name // empty')
        if [ -z "$model_names" ]; then
            echo "No models found. Run: oramalama pull"
            exit 1
        fi
        model=$(pick_model "Select a model to chat with" "$models_json") || exit 0
    fi

    if $DRY_RUN; then
        local flags=""
        $THINK        && flags+=" --think"
        $HIDETHINKING && flags+=" --hidethinking"
        $VERBOSE      && flags+=" --verbose"
        echo "[dry-run] oramalama run${flags} ${model}"
        return
    fi

    # Start server (sets ENDPOINT, SERVED_MODEL_ID, CTX_SIZE)
    CLI_MODEL="$model"
    ensure_server_running

    # ── Session state ────────────────────────────────────────────────────────
    local msgs_file params_file
    msgs_file=$(mktemp --suffix=.json)
    params_file=$(mktemp --suffix=.json)
    echo "[]" > "$msgs_file"
    echo "{}"  > "$params_file"
    local system_prompt="" in_think=false
    trap 'rm -f "$msgs_file" "$params_file"' EXIT INT TERM

    gum style \
        --border normal --padding "0 1" --border-foreground 212 \
        "$(gum style --bold --foreground 212 "oramalama run") ─ ${SERVED_MODEL_ID}" \
        "Slash commands: /help   Exit: /bye or Ctrl-C"

    # ── REPL loop ────────────────────────────────────────────────────────────
    while true; do
        local user_input
        read -e -r -p $'\033[38;5;212m>>> \033[0m' user_input || break
        [ -z "$user_input" ] && continue
        history -s "$user_input"

        # ── Slash commands ────────────────────────────────────────────────────
        case "$user_input" in
            /bye|/quit|/exit) break ;;

            /help)
                cat <<'HELP'
  /set system <text>       Set the system prompt  (/set system  to clear)
  /set parameter <k> <v>  Set a generation param  (temperature, top_p, …)
  /clear                  Wipe conversation history
  /save <name>            Save session to ~/.config/oramalama/sessions/<name>
  /load <name>            Load a saved session
  /show                   Print current model, system prompt, params
  /switch                 Switch the running model (local or remote via SSH)
  /bye                    Exit
HELP
                continue ;;

            /clear)
                echo "[]" > "$msgs_file"
                echo "{}"  > "$params_file"
                system_prompt="" in_think=false
                echo "🗑️  Cleared."
                continue ;;

            "/set system")
                system_prompt=""
                echo "✅ System prompt cleared."
                continue ;;
            "/set system "*)
                system_prompt="${user_input#/set system }"
                echo "✅ System prompt set."
                continue ;;

            "/set parameter "*)
                local kv="${user_input#/set parameter }"
                local pk="${kv%% *}" pv="${kv#* }"
                local tmp_p; tmp_p=$(mktemp --suffix=.json)
                jq --arg k "$pk" \
                   --argjson v "$(jq -rn --arg v "$pv" '$v | tonumber? // .' 2>/dev/null || echo "\"$pv\"")" \
                   '.[$k] = $v' "$params_file" > "$tmp_p" \
                    && mv "$tmp_p" "$params_file" \
                    || echo "⚠️  Could not parse value: ${pv}"
                echo "✅ ${pk} = ${pv}"
                continue ;;

            "/save "*)
                local sname="${user_input#/save }"
                local sdir="$HOME/.config/oramalama/sessions"
                mkdir -p "$sdir"
                cp "$msgs_file" "${sdir}/${sname}.json"
                echo "💾 Saved → ${sdir}/${sname}.json"
                continue ;;

            "/load "*)
                local lname="${user_input#/load }"
                local lfile="$HOME/.config/oramalama/sessions/${lname}.json"
                if [ -f "$lfile" ]; then
                    cp "$lfile" "$msgs_file"
                    in_think=false
                    echo "📂 Loaded ${lname}."
                else
                    echo "❌ Not found: ${lfile}"
                fi
                continue ;;

            /show)
                echo "Model   : ${SERVED_MODEL_ID}"
                echo "Context : ${CTX_SIZE} tokens"
                echo "Endpoint: ${ENDPOINT}"
                [ -n "$REMOTE_SSH_HOST" ] && echo "SSH     : ${REMOTE_SSH_HOST}"
                echo "System  : ${system_prompt:-<none>}"
                echo "Params  : $(cat "$params_file")"
                continue ;;

            /switch)
                local switch_names switch_json switch_model
                if [ -n "$REMOTE_SSH_HOST" ]; then
                    echo "📋 Listing models on ${REMOTE_SSH_HOST}..."
                    switch_json=$(_remote_list_models "$REMOTE_SSH_HOST")
                    switch_names=$(echo "$switch_json" | jq -r '.[].name // empty' 2>/dev/null)
                    if [ -z "$switch_names" ]; then
                        echo "❌ No models found on ${REMOTE_SSH_HOST}"
                        continue
                    fi
                    switch_model=$(echo "$switch_names" \
                        | gum choose --header "Switch model on ${REMOTE_SSH_HOST}:") || { continue; }
                    [ -n "$switch_model" ] && \
                        _remote_switch_model "$REMOTE_SSH_HOST" "$switch_model"
                else
                    # Local switch
                    switch_json=$(get_models_json)
                    switch_names=$(echo "$switch_json" | jq -r '.[].name // empty' 2>/dev/null)
                    if [ -z "$switch_names" ]; then
                        echo "❌ No local models found"
                        continue
                    fi
                    switch_model=$(echo "$switch_names" \
                        | gum choose --header "Switch model:") || { continue; }
                    if [ -n "$switch_model" ] && [[ "$switch_model" != "$SERVED_MODEL_ID" ]]; then
                        gum style --foreground 212 \
                            "🔄 Switching: ${SERVED_MODEL_ID} → ${switch_model}"
                        if systemctl --user is-active --quiet "$QUADLET_SERVICE" 2>/dev/null; then
                            systemctl --user stop "$QUADLET_SERVICE"
                        else
                            ramalama stop "$CONTAINER_NAME" 2>/dev/null || true
                        fi
                        sleep 2
                        ramalama serve --detach --name "$CONTAINER_NAME" "$switch_model"
                        wait_for_server "$ENDPOINT"
                        local new_id
                        new_id=$(get_model_id_from_endpoint "$ENDPOINT")
                        SERVED_MODEL_ID="${new_id:-$switch_model}"
                        # Clear history — different model, fresh context
                        echo "[]" > "$msgs_file"; echo "{}" > "$params_file"
                        system_prompt="" in_think=false
                        gum style --foreground 121 "✅ Now using: ${SERVED_MODEL_ID}"
                    fi
                fi
                continue ;;
        esac

        # ── Build request ────────────────────────────────────────────────────
        local messages
        messages=$(cat "$msgs_file")
        messages=$(jq --arg c "$user_input" '. + [{"role":"user","content":$c}]' <<< "$messages")

        local request
        request=$(jq -n \
            --arg     model   "$SERVED_MODEL_ID" \
            --argjson msgs    "$messages" \
            --argjson params  "$(cat "$params_file")" \
            --argjson think   "$THINK" \
            '$params + {model: $model, stream: true, messages: $msgs} |
             if $think then . + {thinking: true} else . end')
        if [ -n "$system_prompt" ]; then
            request=$(jq --arg sys "$system_prompt" \
                '.messages = [{"role":"system","content":$sys}] + .messages' <<< "$request")
        fi

        # ── Stream response ──────────────────────────────────────────────────
        local full_response="" tok_per_sec="" prompt_n="" predicted_n=""
        in_think=false
        printf '\n'

        while IFS= read -r sse_line; do
            [[ "$sse_line" == "data: [DONE]" ]] && break
            [[ "$sse_line" != "data: "* ]]      && continue
            local chunk="${sse_line#data: }"

            # Reasoning/thinking delta (Gemma 4 / DeepSeek-R1 reasoning_content field)
            local rdelta
            rdelta=$(jq -r '.choices[0].delta.reasoning_content // empty' <<< "$chunk" 2>/dev/null)
            if [ -n "$rdelta" ] && ! $HIDETHINKING; then
                printf '\033[2m%s\033[0m' "$rdelta"
            fi

            # Content delta — also handles inline <think>…</think> blocks
            local cdelta
            cdelta=$(jq -r '.choices[0].delta.content // empty' <<< "$chunk" 2>/dev/null)
            if [ -n "$cdelta" ]; then
                if $HIDETHINKING; then
                    # State-machine: strip <think>…</think> that may span chunks
                    local rest="$cdelta"
                    while [ -n "$rest" ]; do
                        if $in_think; then
                            if [[ "$rest" == *"</think>"* ]]; then
                                in_think=false
                                rest="${rest#*</think>}"
                            else
                                rest=""
                            fi
                        else
                            if [[ "$rest" == *"<think>"* ]]; then
                                local visible="${rest%%<think>*}"
                                printf '%s' "$visible"
                                full_response+="$visible"
                                in_think=true
                                rest="${rest#*<think>}"
                            else
                                printf '%s' "$rest"
                                full_response+="$rest"
                                rest=""
                            fi
                        fi
                    done
                else
                    printf '%s' "$cdelta"
                    full_response+="$cdelta"
                fi
            fi

            # Timings are in the final (finish_reason) chunk
            local raw_tps
            raw_tps=$(jq -r '.timings.predicted_per_second // empty' <<< "$chunk" 2>/dev/null)
            if [ -n "$raw_tps" ]; then
                tok_per_sec="$raw_tps"
                prompt_n=$(jq -r    '.timings.prompt_n    // 0' <<< "$chunk" 2>/dev/null)
                predicted_n=$(jq -r '.timings.predicted_n // 0' <<< "$chunk" 2>/dev/null)
            fi
        done < <(curl -sN \
            -H "Content-Type: application/json" \
            -d "$request" \
            "${ENDPOINT}/v1/chat/completions" 2>/dev/null)

        printf '\n'
        if $VERBOSE && [ -n "$tok_per_sec" ]; then
            printf '\033[2m📊 %.1f tok/s  ·  %s prompt tokens  ·  %s generated\033[0m\n' \
                "$tok_per_sec" "$prompt_n" "$predicted_n"
        fi
        printf '\n'

        # Append assistant turn to history
        messages=$(jq --arg c "$full_response" '. + [{"role":"assistant","content":$c}]' <<< "$messages")
        echo "$messages" > "$msgs_file"
    done

    rm -f "$msgs_file" "$params_file"
}

cmd_serve() {
    local model="${1:-$CLI_MODEL}"

    if [ -n "$REMOTE_ENDPOINT" ]; then
        echo "ℹ️  Using remote server at ${REMOTE_ENDPOINT}"
        get_ramalama_endpoint
        return
    fi

    if [ -z "$model" ]; then
        local models_json model_names
        models_json=$(get_models_json)
        model_names=$(echo "$models_json" | jq -r '.[].name // empty')
        if [ -z "$model_names" ]; then
            echo "❌ No models found. Run: oramalama pull"
            exit 1
        fi
        model=$(pick_model "Select a model to serve" "$models_json") || exit 0
    fi

    SELECTED_MODEL="$model"
    local ctx
    ctx=$(get_ctx_size "$SELECTED_MODEL")
    CTX_SIZE="$ctx"
    echo "📐 Context window: ${CTX_SIZE} tokens"

    local models_json model_size_bytes model_size_gb required_gb
    models_json=$(get_models_json)
    model_size_bytes=$(echo "$models_json" | jq -r --arg n "$SELECTED_MODEL" '.[] | select(.name==$n) | .size // 0')
    model_size_gb=$(( ${model_size_bytes:-0} / 1024 / 1024 / 1024 ))
    required_gb=$(( model_size_gb + 4 ))
    detect_gtt
    if [ "$model_size_gb" -gt "$GTT_TOTAL_GB" ] && [ "$GTT_TOTAL_GB" -gt 0 ]; then
        gum style --border double --border-foreground 196 --margin "1" --padding "1" \
            "🚫 MODEL TOO LARGE" \
            "Model: ${model_size_gb}GB  GPU pool: ${GTT_TOTAL_GB}GB"
        exit 1
    fi
    check_resources "$required_gb"

    echo "🚀 Starting server with ${SELECTED_MODEL}..."
    if [[ "$SELECTED_MODEL" == "$DEFAULT_MODEL" ]] && systemctl --user list-unit-files --quiet "$QUADLET_SERVICE" &>/dev/null; then
        dry_run_cmd "systemctl --user start '${QUADLET_SERVICE}'"
    else
        local image_flag=""
        [ -n "$USE_IMAGE" ] && image_flag="--image ${USE_IMAGE}"
        dry_run_cmd "run_cmd 'ramalama serve --detach --name ${CONTAINER_NAME} ${image_flag} -c ${CTX_SIZE} --runtime-args=\"${EXTRA_SERVE_FLAGS}\" ${SELECTED_MODEL}'"
    fi
    sleep 2

    local endpoint
    endpoint=$(get_ramalama_endpoint)
    if ! $DRY_RUN && ! wait_for_server "$endpoint"; then
        echo "❌ Server failed to start."
        exit 1
    fi
    echo "🟢 Server ready"
    gum style --foreground 39 \
        "   🌐 Web UI → ${endpoint}" \
        "   🔌 API    → ${endpoint}/v1"
    ENDPOINT="$endpoint"
}

cmd_search() {
    if ! command -v llmfit &>/dev/null; then
        gum style --foreground 196 \
            "⚠️  llmfit is not installed. Install it for model recommendations:" \
            "    https://github.com/jmorganca/llmfit"
        return 1
    fi

    # Read actual GTT total from sysfs — reflects BIOS VRAM allocation (e.g. 96GB)
    # This is more accurate than system RAM which the OS sees after BIOS split.
    detect_gtt
    local MEM_FLAG="${GTT_TOTAL_GB}G"
    gum style --foreground 212 "🤖 Querying llmfit for hardware-optimal models (Strix Halo, ${GTT_TOTAL_GB}GB GPU pool)..."

    local LLMFIT_JSON
    LLMFIT_JSON=$(llmfit \
        --memory "${MEM_FLAG}" \
        recommend \
        --use-case coding \
        --min-fit good \
        -n 8 \
        --json 2>/dev/null)

    if [ -z "$LLMFIT_JSON" ]; then
        gum style --foreground 196 "⚠️  llmfit did not return results."
        return
    fi

    # Build a human-readable list: "name (Xgb, ~Y tok/s) [best_quant]"
    local SUGGESTIONS
    SUGGESTIONS=$(echo "$LLMFIT_JSON" | jq -r '
        .models[] |
        "\(.name)  (\(.memory_required_gb | floor)GB, ~\(.estimated_tps | floor) tok/s, \(.best_quant))"
    ')

    gum style --bold "💡 llmfit recommendations for your hardware:"
    echo "$SUGGESTIONS" | gum style --foreground 105

    if gum confirm "Would you like to pull one of these recommended models via ramalama?"; then
        local SELECTED_LINE
        SELECTED_LINE=$(echo "$SUGGESTIONS" | gum choose --header "Select a model to pull")
        [ -z "$SELECTED_LINE" ] && return

        # Extract original name (before the parenthetical suffix)
        local SELECTED_LLMFIT_NAME
        SELECTED_LLMFIT_NAME=$(echo "$SELECTED_LINE" | sed 's/  (.*//')

        # Find best_quant for this model
        local BEST_QUANT
        BEST_QUANT=$(echo "$LLMFIT_JSON" | jq -r \
            --arg n "$SELECTED_LLMFIT_NAME" \
            '.models[] | select(.name == $n) | .best_quant')

        # Find a gguf_source repo (prefer unsloth or bartowski)
        local GGUF_REPO
        GGUF_REPO=$(echo "$LLMFIT_JSON" | jq -r \
            --arg n "$SELECTED_LLMFIT_NAME" \
            '.models[] | select(.name == $n) | .gguf_sources[]
             | select(.provider == "unsloth" or .provider == "bartowski")
             | .repo' | head -1)

        local PULL_REF
        if [ -n "$GGUF_REPO" ]; then
            PULL_REF="hf://${GGUF_REPO}:${BEST_QUANT}"
        else
            # No known GGUF — try huggingface generic path
            PULL_REF="hf://${SELECTED_LLMFIT_NAME}:${BEST_QUANT}"
        fi

        gum style --foreground 212 "📥 Pulling ${PULL_REF} via ramalama..."
        run_cmd "ramalama pull ${PULL_REF}" && \
            gum style --foreground 121 "✅ Model pulled! Restart the script to use it." || \
            gum style --foreground 196 "❌ Pull failed. Check ramalama pull output above."
        exit 0
    fi
}

# Backward-compat alias
suggest_models_llmfit() { cmd_search; }

# ---------------------------------------------------------------------------
# _launch_container: generic helper — start a UI container pointed at the
# ramalama API, then open a browser tab.  Containers can't reach 127.0.0.1
# on the host, so callers should pass CONTAINER_API (already rewritten) in
# env var values rather than the raw ENDPOINT.
#
# Usage: _launch_container <display> <cname> <image> <host:cport> KEY=VALUE …
# ---------------------------------------------------------------------------
_launch_container() {
    local display_name="$1" cname="$2" image="$3" port_map="$4"
    shift 4  # remaining: KEY=VALUE env pairs

    local engine
    engine=$(command -v podman 2>/dev/null || command -v docker 2>/dev/null || true)
    if [ -z "$engine" ]; then
        gum style --foreground 196 "❌ podman or docker is required for ${display_name}"
        return 1
    fi

    local host_port="${port_map%%:*}"

    if "$engine" ps --format '{{.Names}}' 2>/dev/null | grep -qx "$cname"; then
        gum style --foreground 121 "✅ ${display_name} already running → http://localhost:${host_port}"
    else
        gum style --foreground 212 "🐋 Starting ${display_name} (${image})..."
        local env_flags=""
        for kv in "$@"; do env_flags+=" -e '${kv}'"; done
        dry_run_cmd "$engine run -d --rm --name '${cname}' -p ${port_map}${env_flags} '${image}'"
        $DRY_RUN || gum spin --spinner dot --title "Waiting for ${display_name}..." -- sleep 6
    fi

    gum style --foreground 39 "🌐 ${display_name} → http://localhost:${host_port}"
    dry_run_cmd "xdg-open 'http://localhost:${host_port}'"
}

cmd_launch() {
    local launch_tool=""
    while [[ $# -gt 0 ]]; do
        case "$1" in
            --tool)    launch_tool="$2"; shift 2 ;;
            --suggest) cmd_search; return ;;
            *)         break ;;
        esac
    done

    [ -z "$launch_tool" ] && launch_tool="${DEFAULT_TOOL:-}"
    [ -z "$launch_tool" ] && launch_tool="${CLI_TOOL:-}"

    # ── Tool selection ───────────────────────────────────────────────────────
    local SELECTED_TOOL
    if [ -n "$launch_tool" ]; then
        # Direct --tool dispatch (no interactive menu)
        case "${launch_tool,,}" in
            opencode)                  SELECTED_TOOL="OpenCode" ;;
            goose)                     SELECTED_TOOL="Goose CLI" ;;
            vscode|code)               SELECTED_TOOL="VS Code" ;;
            aichat)                    SELECTED_TOOL="aichat" ;;
            tgpt)                      SELECTED_TOOL="tgpt" ;;
            webui|web-ui|web_ui)       SELECTED_TOOL="Web UI" ;;
            open-webui|openwebui)      SELECTED_TOOL="Open WebUI" ;;
            anythingllm)               SELECTED_TOOL="AnythingLLM" ;;
            nextchat|next-chat)        SELECTED_TOOL="NextChat" ;;
            lobe-chat|lobechat)        SELECTED_TOOL="Lobe Chat" ;;
            big-agi|bigagi)            SELECTED_TOOL="big-AGI" ;;
            server|only*)              SELECTED_TOOL="Only Start Server" ;;
            suggest|llmfit)            cmd_search; return ;;
            *)                         echo "❌ Unknown tool: $launch_tool"; usage ;;
        esac
    else
        # Two-level interactive menu: category → specific tool
        local has_container=false
        { command -v podman &>/dev/null || command -v docker &>/dev/null; } && has_container=true

        local CATS=("Coding Tools" "Web UIs" "Terminal Chat" "Only Start Server")
        command -v llmfit &>/dev/null && CATS+=("Suggest Models (llmfit)")
        local CATEGORY
        CATEGORY=$(printf "%s\n" "${CATS[@]}" \
            | gum choose --header "What would you like to launch?")
        [ -z "$CATEGORY" ] && exit 0

        case "$CATEGORY" in
            "Coding Tools")
                local T=()
                command -v opencode &>/dev/null && T+=("OpenCode")
                T+=("Goose CLI")
                command -v code &>/dev/null && T+=("VS Code  (Continue / Cline plugin)")
                [ ${#T[@]} -eq 0 ] && echo "No coding tools found. Install opencode, goose, or code." && exit 1
                SELECTED_TOOL=$(printf "%s\n" "${T[@]}" | gum choose --header "Select a coding tool")
                ;;
            "Web UIs")
                local T=("Web UI  (llama.cpp built-in, no setup needed)")
                if $has_container; then
                    T+=(
                        "Open WebUI     port ${OPEN_WEBUI_PORT:-3000}"
                        "AnythingLLM    port 3001"
                        "NextChat       port 3002"
                        "Lobe Chat      port 3210"
                        "big-AGI        port 3003"
                    )
                else
                    T+=("(install podman or docker to enable container UIs)")
                fi
                SELECTED_TOOL=$(printf "%s\n" "${T[@]}" \
                    | gum choose --header "Select a Web UI")
                [[ "$SELECTED_TOOL" == "("* ]] && exit 0  # placeholder line
                ;;
            "Terminal Chat")
                local T=()
                command -v aichat &>/dev/null && T+=("aichat")
                command -v tgpt   &>/dev/null && T+=("tgpt")
                [ ${#T[@]} -eq 0 ] && echo "No terminal chat tools found. Install aichat or tgpt." && exit 1
                SELECTED_TOOL=$(printf "%s\n" "${T[@]}" | gum choose --header "Select a terminal chat tool")
                ;;
            "Only Start Server")
                SELECTED_TOOL="Only Start Server" ;;
            "Suggest Models (llmfit)")
                cmd_search; return ;;
        esac
        [ -z "$SELECTED_TOOL" ] && exit 0
    fi

    [ "$SELECTED_TOOL" = "Suggest Models (llmfit)" ] && { cmd_search; return; }

    # ── Start server ─────────────────────────────────────────────────────────
    ensure_server_running

    # Container networking: rewrite 127.0.0.1/localhost so containers can
    # reach the host API endpoint (podman/docker host gateway).
    local CONTAINER_API="${ENDPOINT//127.0.0.1/host.containers.internal}"
    CONTAINER_API="${CONTAINER_API//localhost/host.containers.internal}"

    # ── Tool execution ───────────────────────────────────────────────────────
    case "$SELECTED_TOOL" in

        # ── Coding tools ─────────────────────────────────────────────────────
        "OpenCode")
            local DISPLAY_NAME
            DISPLAY_NAME=$(echo "$SERVED_MODEL_ID" | sed 's|hf://||; s|:| |g; s|/| / |g')
            configure_opencode "$ENDPOINT" "$SERVED_MODEL_ID" "$DISPLAY_NAME (RamaLama)"
            gum style --foreground 212 "💻 Launching OpenCode..."
            dry_run_cmd "opencode $*"
            ;;
        "Goose CLI")
            gum style --foreground 212 "🦢 Launching Goose CLI..."
            dry_run_cmd "GOOSE_PROVIDER=openai GOOSE_MODEL='${SERVED_MODEL_ID}' OPENAI_HOST='${ENDPOINT}' OPENAI_API_KEY='sk-no-key-required' goose session"
            ;;
        "VS Code"*)
            gum style --foreground 212 "💡 Opening VS Code..."
            gum style --foreground 245 "   Install the Continue or Cline extension and point it at ${ENDPOINT}/v1"
            dry_run_cmd "code ."
            return
            ;;

        # ── Terminal chat ─────────────────────────────────────────────────────
        "aichat")
            gum style --foreground 212 "💬 Launching aichat..."
            dry_run_cmd "OPENAI_API_BASE='${ENDPOINT}/v1' OPENAI_API_KEY='sk-no-key-required' aichat"
            ;;
        "tgpt")
            gum style --foreground 212 "💬 Launching tgpt..."
            dry_run_cmd "tgpt --provider openai --url '${ENDPOINT}/v1/chat/completions' --key 'sk-no-key-required' --model '${SERVED_MODEL_ID}'"
            ;;

        # ── Web UIs ───────────────────────────────────────────────────────────
        "Web UI"*)
            gum style --foreground 212 "🌐 Opening llama.cpp built-in Web UI..."
            gum style --foreground 39 "   ${ENDPOINT}"
            dry_run_cmd "xdg-open '${ENDPOINT}'"
            return
            ;;
        "Open WebUI"*)
            _launch_container "Open WebUI" "open-webui-oramalama" \
                "ghcr.io/open-webui/open-webui:main" \
                "${OPEN_WEBUI_PORT:-3000}:8080" \
                "OPENAI_API_BASE_URL=${CONTAINER_API}/v1" \
                "OPENAI_API_KEY=sk-no-key-required"
            return
            ;;
        "AnythingLLM"*)
            _launch_container "AnythingLLM" "anythingllm-oramalama" \
                "mintplexlabs/anythingllm" \
                "3001:3001" \
                "LLM_PROVIDER=openai" \
                "OPEN_AI_KEY=sk-no-key-required" \
                "OPEN_MODEL_PREF=${SERVED_MODEL_ID}" \
                "OPENAI_BASE_PATH=${CONTAINER_API}/v1"
            return
            ;;
        "NextChat"*)
            _launch_container "NextChat" "nextchat-oramalama" \
                "yidadaa/chatgpt-next-web" \
                "3002:3000" \
                "OPENAI_API_KEY=sk-no-key-required" \
                "BASE_URL=${CONTAINER_API}"
            return
            ;;
        "Lobe Chat"*)
            _launch_container "Lobe Chat" "lobechat-oramalama" \
                "lobehub/lobe-chat" \
                "3210:3210" \
                "OPENAI_API_KEY=sk-no-key-required" \
                "OPENAI_PROXY_URL=${CONTAINER_API}/v1"
            return
            ;;
        "big-AGI"*)
            _launch_container "big-AGI" "bigagi-oramalama" \
                "ghcr.io/enricoros/big-agi" \
                "3003:3000" \
                "OPENAI_API_HOST=${CONTAINER_API}" \
                "OPENAI_API_KEY=sk-no-key-required"
            return
            ;;

        "Only Start Server")
            gum style --foreground 121 \
                "✅ Server ready" \
                "   🌐 Web UI → ${ENDPOINT}" \
                "   🔌 API    → ${ENDPOINT}/v1"
            return
            ;;
    esac

    # ── Cleanup for blocking (foreground) tools ───────────────────────────────
    # Container-based UIs (web) return early above so this only runs for
    # CLI tools (OpenCode, Goose, aichat, tgpt) that hold the terminal.
    if [ -z "$REMOTE_HOST" ] && [ -z "$REMOTE_ENDPOINT" ]; then
        sleep 1
        if ! pgrep -f "bin/opencode|goose|claude|copilot|aichat|tgpt" > /dev/null; then
            if systemctl --user is-active --quiet "$QUADLET_SERVICE" 2>/dev/null; then
                echo "💤 Last instance closed. Leaving systemd service running (use: oramalama stop)"
            else
                echo "💤 Last instance closed. Stopping RamaLama..."
                dry_run_cmd "ramalama stop '${CONTAINER_NAME}'"
            fi
        fi
    fi
}

# ============================================================================
# usage
# ============================================================================
usage() {
    cat <<EOF
Usage: oramalama [--remote <url>] [--model <name>] [--dry-run] <subcommand> [args]

Subcommands:
  run [model]        Interactive chat REPL (starts server if needed)
                       --think          Show model reasoning/thinking
                       --hidethinking   Hide <think> blocks from output
                       --verbose        Print tok/s stats after each reply
  serve [model]      Start the inference server (detached)
  launch             Start server + launch a tool (two-level menu)
                       --tool opencode    OpenCode (coding)
                       --tool goose       Goose CLI (coding)
                       --tool vscode      VS Code — for Continue/Cline
                       --tool aichat      aichat (terminal chat)
                       --tool tgpt        tgpt (terminal chat)
                       --tool webui       llama.cpp built-in Web UI (browser)
                       --tool open-webui  Open WebUI container (port 3000)
                       --tool anythingllm AnythingLLM container (port 3001)
                       --tool nextchat    NextChat container (port 3002)
                       --tool lobe-chat   Lobe Chat container (port 3210)
                       --tool big-agi     big-AGI container (port 3003)
  list               List downloaded models
  pull [model]       Pull a model (suggests best fit if no model given)
  ps                 Show running models
  stop [container]   Stop the running server or a specific container
  show [model]       Show model metadata (arch, quant, size, context)
  rm [model]         Remove a model
  search             Search and recommend models via llmfit
  discover           Scan local network + Tailscale for ramalama servers

Global flags:
  --remote <url>     Use a remote ramalama server (e.g. http://myserver:8080)
  --model <name>     Model to use (skips interactive picker)
  --dry-run          Print actions without executing them

Config: ~/.config/oramalama/config (bash key=value)
  RAMALAMA_ENDPOINT  Same as --remote
  DEFAULT_MODEL      Override default model
  DEFAULT_TOOL       Pre-select tool for launch (opencode, goose)
EOF
    exit 0
}

# ============================================================================
# GLOBAL FLAG PARSING + SUBCOMMAND DISPATCH
# ============================================================================

SUBCOMMAND="launch"   # default
DRY_RUN=false
CLI_MODEL=""
CLI_TOOL=""
REMOTE_SSH_HOST=""
REMOTE_ENDPOINT="${RAMALAMA_ENDPOINT:-}"

while [[ $# -gt 0 ]]; do
    case "$1" in
        --remote)   RAMALAMA_ENDPOINT="$2"; REMOTE_ENDPOINT="$2"; shift 2 ;;
        --dry-run)  DRY_RUN=true; shift ;;
        --model)    CLI_MODEL="$2"; shift 2 ;;
        --help|-h)  usage ;;
        --)         shift; break ;;
        -*)         echo "Unknown flag: $1"; usage ;;
        *)          SUBCOMMAND="$1"; shift; break ;;
    esac
done

[[ "$SUBCOMMAND" =~ ^(help|--help|discover)$ ]] || check_deps
[ -z "$REMOTE_ENDPOINT" ] && detect_hardware

case "$SUBCOMMAND" in
    run)          cmd_run "$@" ;;
    serve)        cmd_serve "$@" ;;
    launch)       cmd_launch "$@" ;;
    list|ls)      cmd_list "$@" ;;
    pull)         cmd_pull "$@" ;;
    ps)           cmd_ps "$@" ;;
    stop)         cmd_stop "$@" ;;
    show)         cmd_show "$@" ;;
    rm)           cmd_rm "$@" ;;
    search)       cmd_search "$@" ;;
    discover)     cmd_discover true "$@" ;;
    help|--help)  usage ;;
    *)            echo "❌ Unknown subcommand: $SUBCOMMAND"; usage ;;
esac
